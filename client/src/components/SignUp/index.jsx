import React from 'react';

// create component functio SignUp, for SignUp page, so it can be used with react hook
function SignUp(props){
    return(
        <div>
        SignUp Here!
        </div>
    )
}

// export to be routed in main App 
export default SignUp 